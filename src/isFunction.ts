/**
 * @name isFunction
 * @param {val} any
 * @returns boolean
 * @since 1.0.0
 * @section
 * @example
 */
const isFunction = (val: any) => {
  return typeof val === 'function';
};

export default isFunction;
